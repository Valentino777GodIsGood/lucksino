# ═══════════════════════════════════════════════════════════════
# Lucksino — Production Docker Compose
# Run: docker compose up --build
# ═══════════════════════════════════════════════════════════════

services:
  # ─── Game Server (Colyseus + Express API) ─────────────────────
  server:
    build:
      context: .
      dockerfile: Dockerfile
      target: server
    ports:
      - "2567:2567"
    environment:
      - NODE_ENV=production
      - PORT=2567
      - APP_URL=${APP_URL:-http://localhost:3000}
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:2567/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ─── Client (Nginx serving Vite build) ────────────────────────
  client:
    build:
      context: .
      dockerfile: Dockerfile
      target: client
    ports:
      - "3000:80"
    depends_on:
      server:
        condition: service_healthy
    restart: unless-stopped

  # ─── (Optional) Redis for session persistence at scale ────────
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

# volumes:
#   redis_data:
